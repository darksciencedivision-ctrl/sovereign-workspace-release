# Approved top-level input for the hash-locked SOVEREIGN runtime lock (CR-034).
# Compile with: pip-compile --generate-hashes --output-file requirements.lock.txt requirements.in
flask==3.1.3
requests==2.34.2
chromadb==1.5.9
numpy==2.5.1
pydantic==2.13.4
