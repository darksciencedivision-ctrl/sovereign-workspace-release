/**
 * Product version is intentionally read from `/v1/health` at runtime. Keeping
 * another embedded product version in the SPA would create a split authority.
 */
export const DOCS_PATH = "docs/SOVEREIGN_CLIENT_ADAPTER_CONTRACT.md";
