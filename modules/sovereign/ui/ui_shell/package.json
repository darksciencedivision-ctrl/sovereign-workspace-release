{
  "name": "sovereign-ui",
  "private": true,
  "version": "3.1.2",
  "description": "SOVEREIGN canonical same-origin product client.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "test": "node -e \"console.error('CR-030: UI unit tests were intentionally removed in the source-only tree. This lane is UNSUPPORTED (non-qualifying); the UI is covered by typecheck + build. Re-add a vitest suite to restore it.'); process.exit(1)\""
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^6.0.4",
    "jsdom": "^30.0.0",
    "typescript": "^5.6.3",
    "vite": "^8.1.5",
    "vitest": "^4.1.11"
  }
}
