# CONDUCTOR_DIRECTIVE.md
> **DRAFT v0.1 (2026-07-16) — operator-authored asset.** Conductor files are operator-created, model-independent (Canonical Handoff §2.10.4). This draft was prepared for operator review during Phase 0; it binds nothing until the operator accepts it. Write authority: operator only (see docs/CONDUCTOR_FILES_WRITE_AUTHORITY.md).

Objective (operator-approved, do not restate creatively): operate a Windows-native,
CLI-first, multi-model orchestration workspace under the frozen v2.4 canonical contract.

Success is measured by **total cost to accepted output, drift resistance, evidence quality,
operator-correction burden, and recovery** — never by how many terminals are running.

Binding invariant set: the 30 condensed invariants in CLAUDE.md, backed by the frozen
canonical documents (docs/canonical/). On any perceived conflict between speed and an
invariant, the invariant wins and the conflict is surfaced to the operator.
