# PHASE 16F — EVIDENCE REPORT (whole-track close)

**Work unit:** `phase-16f.close` (sub-step 2 of 2 — **`.assembled` → `.close`**; this unit closes the
whole Phase-16F track).
**Date:** 2026-07-25 · **Iteration:** 70 · **Status:** PASS (HIGH-STAKES gate — mandatory
independent `gate-validator` confirmation obtained).
**Tag:** `gate/phase-16f` **and** the Phase-16 terminal tag `product/usable` land with this unit.
**Governing directive:** `AUTONOMOUS_BUILD_DIRECTIVE.md` **§15 track 16F** (*"Assembled run in the
operator's shell: type to the live conductor, watch it spawn/drive picker-selected workers in panes,
approval exercised, restart recovery — receipts + FINAL report addendum + `product/usable`. High-stakes
gate."*), **§13 (OP-8 — the conductor is a live conversational pane)**, **§14 (OP-9 — live terms
confirmed)**, loop protocol §3, substitution §6, honesty §10.4, **D-P16-0 binding** (every shell/UI
change exercised by an in-Electron autorun self-check writing a machine-readable receipt).
Load-bearing invariants: **1** (operator holds final authority — the app never self-authorizes),
**2** (no naked session — every pane supervised-admitted), **16** (explicit gates, no override),
**18** (no node judges its own work), **3/24** (fail-closed; a substituted/mock surface is always
visibly named, never overclaimed), **26** (transcribe-then-discard), **I-V2/D-VOICE-02** (no TTS).

---

## 1. What `.close` does (directive §3.2 whole-track-gate convention)

16F was decomposed `.assembled` → `.close` (same convention as 14A/15E/16C/16D/16E: the gate tag lands
only when the final sub-step closes with the mandatory review). `.assembled` (iter 69, work `db88797`,
evidence `7d6bc1b`) built the assembled in-Electron self-check and proved every leg composes in ONE
packaged Electron session. `.close` (this unit):

1. **Re-ran the assembled self-check FRESH, foreground** on this host (D-P16-0 + D-LOOP-2 —
   never adopt a prior turn's receipt).
2. **Re-ran the full test suites FRESH, foreground.**
3. **Obtained the mandatory independent `gate-validator` confirmation** on the whole 16F track
   (16F **is** a directive-flagged high-stakes gate) + re-ran `spec-auditor`.
4. Wrote this report + the **Phase-16 FINAL report addendum** (`FINAL_LIVE_REPORT.md`).
5. Tags `gate/phase-16f` + `product/usable`; sets the Phase-16 terminal state.

No new product code this unit — `.close` is re-verification + reviews + evidence + tags. The reviewed
artifact is the `.assembled` composition (`apps/desktop/selfcheck/assembled-selfcheck.js` + the 5 wiring
edits in `main.js`/`run.js`) plus the governed feeds it consumes (16B–16E).

## 2. Self-check every exit criterion (real command output, this host, foreground)

**In-Electron assembled self-check** (`node selfcheck/run.js assembled`, from `apps/desktop`;
packaged Electron 31.7.7 / node 20.18.0 / Chrome 126 / win32-x64):

```
[shell] conductor: pane 1 GOVERNED-BORN via spawn_conductor_pane — node_state=awaiting_live_conductor, I-X3 released=true (D-LOOP-1)
[shell] supervision READY (verified control-plane channel)
[shell] conductor: governed DISPATCH ran (mock-first) — 2 worker(s) by descriptor, 2 accepted, legs mock/mock, live workers OWED (U58); torn_down=true (D-LOOP-1)
[shell] [selfcheck:assembled] conductor input delivered + echoed into admitted session pane-2
[shell] picker: operator selected claude_code/Opus 4.8 role=reasoning mode=autonomous target=pane-3 — governed selection RECORDED (dispatch to pane_node_spawn deferred to operator-run/16F)
[shell] [selfcheck:assembled] governed dispatch: 2 accepted by descriptor, legs mock-honest, U58 owed
[shell] approvals: operator approve of ap-3 → governed resolve REFUSED (invariant 16, no override path)
[shell] [selfcheck:assembled] approval drawer sourced (3 rows); governed decide routed + refused (invariant 16)
[shell] [selfcheck:assembled] restart recovery: conductor-first + worker chrome repainted from reconstructed snapshot
[shell] [selfcheck:assembled] PASS → docs/evidence/receipts/PHASE16F_ASSEMBLED_SELFCHECK.json
[selfcheck/run] shell exited code=0 signal=-
```

Receipt `docs/evidence/receipts/PHASE16F_ASSEMBLED_SELFCHECK.json` — **`ok:true`**, regenerated by this
turn's run (started `2026-07-25T19:42…Z`; the committed receipt then carries the `gate-validator`'s own
independent regeneration `19:50…Z` — both this-turn, ok:true). Every leg true:
`legs.{supervision_ready, conductor_governed_born, conductor_input_delivered, workers_dispatched,
picker_worker_selected, approval_exercised, restart_recovered}` all `true`;
`conductor_input.written:true` / `.echoed:true` / `.live_backend_operator_run:true`;
`dispatch.by_descriptor:true` / `.accepted_count:2` / `.acceptance_verdict:"PASS"` /
`.legs_mock_honest:true` / `.live_workers_owed:true` / `.torn_down:true`;
`approval.decide_governed_refused:true` / `.self_authorized_false:true`;
`recovery.{snapshot_carries_chrome, reconstructed_conductor_first, recovered_worker_chrome,
recovered_not_reattached, badge_restored}` all `true`.
**OWED (honest):** live conductor backend (§6 operator-run), live workers (U58), live worker spawn (U70).

**Test suites (foreground, this host):**
- `apps/desktop/test/*.test.js` (`node --test`) → **158 passed / 0 failed**.
- `terminal/test/*.test.js` (`node --test`) → **173 passed / 0 failed**.
- `py -3.12 -m pytest tests/ -q` → **1109 passed / 0 failed** (212.57 s).

## 3. Mandatory independent gate-validator (HIGH-STAKES) — **PASS**

`gate-validator` in an isolated context, trusting nothing:
- **Re-read** directive §15 (track 16F) / §13 / §14; confirmed the exit criteria and the whole-track-gate
  closing convention (no premature per-sub-step tag; `gate/phase-16f` absent pre-close — correct).
- **Re-ran the assembled self-check itself** → exit 0, receipt regenerated with its own timestamps
  (`19:50:01→19:50:06`, vs committed HEAD `18:03:18`), all 7 legs true, `ok:true`.
- **LOAD-BEARING FALSIFICATION:** renamed `tools/live/emit_conductor_dispatch.py` aside → self-check
  **FAILED** (exit 1, `ok:false`, *"dispatch feed not sourced from Python"*, `workers_dispatched`
  fail-closed) → **restored byte-identical** (`sha256 1271b19b…`, `git status` clean, no stray file).
  The leg genuinely exercises the Python governed feed, not a literal.
- **Re-ran the full suites:** pytest **1109 passed**; apps/desktop **158 pass**; terminal **173 pass**.
- **Honesty audit (inv 1/2/3/16/18/24/25/26, I-V2):** no live frontier call (check spawns only
  `powershell.exe` stand-ins; dispatch requires `legs_mock_honest` to pass, so it cannot pass while
  going live); every mock/OWED surface named; approval **governed-refused** with `self_authorized:false`;
  recovery **not** re-attached (`reattach:false`, `admitted:false`); no TTS; **D-LOOP-1** teardown
  confirmed (`torn_down:true`, gateway exited, `tasklist` shows no orphan electron/python).
- **Frozen canonical 4-hash set intact** (`6d3fd03b` / `8c9b7240` / `668089b5` / `cc414372`);
  `git diff 16E-close..HEAD -- docs/canonical mcp_server schemas` **empty** (inv 7 — MCP untouched).

**Verdict: PASS** — 4 **non-blocking** reservations: (R1) `apps/desktop/package-lock.json` is an
untracked `npm install` artifact (deliberately not committed — host-local); (R2) the per-leg `waitFor`
ceilings can theoretically sum above the launcher's 150 s `HARD_TIMEOUT_MS` on a pathologically slow
host → a **false FAIL** (exit 124), fail-closed, never a fabricated PASS (observed ~4–5 s this host);
(R3) the regenerated receipt is committed as the gate evidence by this `.close` commit; (R4) the live
conductor backend (§6), live workers (U58), live worker spawn (U70) remain **OWED / operator-run** by
directive design — the gate closes on the **assembled-shell composition** proof, and the `product/usable`
messaging does **not** overclaim "live." No blocking reservation stands.

## 4. Independent spec-auditor — **CLEAN**

`spec-auditor` (read-only, isolated) on the `.assembled` code + wiring + receipt + evidence:
**CLEAN — no MAJOR/blocking findings, no prohibited drift.** Confirmed inv 1 (approval routes to the
Python authority; `decide_governed_refused` is sourced, not fabricated), inv 2 (both spawns via
supervised `createPaneWithSession`, gated on `isSupervised`; recovery never naked-respawns), inv 16
(a non-approvable clarification cannot be forced through), inv 3/§6 (every stand-in/OWED leg visibly
named, never presented as a real provider), inv 24/25/26 + I-V2 (no TTS/speech-out; voice input-only),
inv 7 (`mcp_server/` untouched), §2 prohibitions (no live frontier call, no credential, no network,
nothing outside repo root, canonical set unmodified), determinism / no float. Two **non-blocking NITs**:
the timeout budget (same as R2, fail-closed) and that `selfAuthorized:false` is a structural constant —
harmless because the real inv-1 proof is the **sourced** `decide_governed_refused`, which fails-closed if
the Python channel is down. No code change taken (would invalidate the fresh independent validation; both
NITs are guarded / by-design).

## 5. Substitutions & OWED (directive §6 / §10.4 — honest, carried to the FINAL addendum)

- **Live interactive `claude` conductor backend — operator-run (§6).** The loop never starts a live
  frontier session autonomously here. Leg 2 proves the shell's conductor-input **delivery + echo** path
  against a supervised stand-in ConPTY; `live_backend_operator_run:true` + the OWED backend recorded.
  Never presented as a live model answer.
- **Live worker CLI legs (U58) / governed worker spawn (U70) — OWED.** Dispatch ran mock-first (honest
  legs, BY DESCRIPTOR, gate-ACCEPTED); the picker selection is recorded and its chrome survives restart;
  the live governed spawn from a selection is the operator-run surface.
- **Real microphone PCM + admitted live interactive conductor ConPTY for the voice→conductor end-to-end**
  — operator-run (16E proved the wiring + real WSL Parakeet transcription live; the mic-into-a-live-
  session composition is operator-seat). Confidence-calibration on the real STT path (presence-based) is
  the recorded 16F calibration item; inv 25 is unaffected (destructive verbs queue by classification).
- **Timeout robustness (R2/NIT)** — recorded; fail-closed.

## 6. Invariants honored

1 (no self-authorization: approvals `selfAuthorized:false`; conductor input is a forwarded CHAT) ·
2 (no naked session: every pane supervised-admitted, gated on `isSupervised`; recovery not re-attached) ·
3/24 (fail-closed; mock/stand-in/OWED surfaces visibly named) · 4 (dispatch routing BY DESCRIPTOR) ·
16 (explicit gate, no override — clarification approve refused) · 18 (the acceptance verdict is the gate
node's `PASS`) · 26 / I-V2 (transcribe-then-discard; no TTS) · 27 (the echo is read from the real xterm
buffer) · D-LOOP-1 (every spawned PTY/gateway/loopback-MCP torn down within the unit; no orphan).

## 7. Two-commit convention (this sub-step)

- **Work commit** (this unit adds no product code): the fresh `.assembled` receipt regenerated this turn.
- **Evidence commit:** this report + the Phase-16 FINAL report addendum (`FINAL_LIVE_REPORT.md`),
  carrying the work-commit hash → tag **`gate/phase-16f`** → tag **`product/usable`**.
- Then the `LOOP_STATE.json` terminal-state commit.

*End of `phase-16f.close` evidence report — Phase 16F whole-track close.*
