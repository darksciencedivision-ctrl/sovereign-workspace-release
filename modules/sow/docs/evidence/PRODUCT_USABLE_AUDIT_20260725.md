# PRODUCT/USABLE AUDIT — independent verification of the Phase-16 completion
**Date:** 2026-07-25 · **Auditors:** Cowork orchestrator + isolated gate-validator (cold
context, refutation-oriented) · **Subject:** `product/usable` at `c00bd97` (final loop
commit `b1c021f`), state COMPLETE iter 70

## Verdict: **USABLE-COMPLETE CONFIRMED WITH RESERVATIONS** — every engineering, lineage, and prohibition claim survived; the register-bookkeeping half was partially refuted and is repaired by backfill in this audit's commit.

## Independently verified (auditor's own runs/reads, not the repo's claims)

**Structure:** all 9 tags present with correct targets (`gate/phase-16a..16f`,
`product/usable`+`gate/phase-16f` → `c00bd97`); fsck clean; zero remotes; two-commit
convention verified for all six tracks; tree clean apart from the two known untracked
host-local files.

**Tests (Linux sandbox):** Python 1109 collected — **1089 passed, 19 skipped, 1
env-coupled failure** (`test_probe_true_on_rc0` requires a `wsl` binary on PATH; passes
19/19 with one present — host-coupled test, not a product defect). JS: **173/173
terminal, 136 pass + 22 env-conditional skips apps/desktop**. Host-side all-green counts
(1368+) are host-conditional, consistent with these.

**Prohibitions:** Phase-16 diff (15e..usable, 96 files) free of credential/API-key
material; `docs/canonical/` and `mcp_server/` untouched (empty diff); frozen manifest
re-verified **42/42 SHA-256 OK**; `config/live_operation.json` gitignored, never
committed, only `.example` tracked; DECISION_REGISTER diff pure-append (+10/−0).

**No product TTS (I-V2/D-VOICE-02):** every synthesis-adjacent hit in product paths is a
negative assertion or the conductor's *result*-synthesis; `ConductorVoiceBridge.speak()`
only transcribes. The `System.Speech` usage exists solely in
`tools/live/real_parakeet_smoke.py` as a labelled TEST-INPUT fixture whose WAV is
unlinked after use — never a product path.

**Receipts:** all 12 `PHASE16*` receipts `ok:true`/`error:null` with honest-negative
fields preserved (16A `echo_dom_seen:false`→U69; 16C dispatch legs mock-honest; 16F owed
block); every receipt timestamp precedes its evidence commit; committed content matches
the working tree.

**Quarantine discipline (2026-07-24 incident note) provably followed:** the 16D.recovery
partials from the crashed iteration were treated as CANDIDATE — suites, in-Electron
self-check, and both subagent reviews re-run fresh before adoption (recorded in the 16d
gate row); the 16F assembled receipt was regenerated by the closer's own fresh run
(`7d6bc1b` 18:03Z vs `9237aba` 19:50Z lineage in git).

**16F validator independence:** PASS + 4 named non-blocking reservations; the
load-bearing falsification (emitter renamed ⇒ self-check fails) and byte-identical
restoration re-verified by the auditor's own re-hash (sha256 `1271b19b…` match).

**Voice honesty:** WSL-side probe fail-closed and timeout-bounded (8 s probe / 180 s
transcribe); mock engine visibly labelled; transcribe-then-discard present; real host
smoke receipt records `nemo:true` detection and transcript "Testing 1234" (conf 1.0)
against `nvidia/parakeet-tdt-0.6b-v3` — host-receipt-trust class (below).

## Reservations (enumerated, dispositioned)

| # | Finding | Severity | Disposition |
|---|---|---|---|
| R1 | DECISION_REGISTER had **no closure rows for `gate/phase-16e` and `gate/phase-16f`/`product/usable`** — the ledger ended at 16d while tags/state reached COMPLETE; recurrence of the 15-era R1 defect | MEDIUM (recording) | **Backfill rows appended with this audit** (dated, marked audit-backfill, citing tags/commits/evidence) |
| R2 | U-register: **U67's `.wire`/`.real` closure never recorded** (block stuck at the `.engine` narrowing despite commits/receipts/tag); and the 16d decision row says "closed U66" while the U-register honestly keeps U66 OPEN-narrowed (persistence/side-effect/identity owed) — the register wording is authoritative, the row overstates | LOW (recording) | **U67 audit-backfill closure block appended**; U66 wording discrepancy recorded, register wording stands ("narrowed", not closed) |
| R3 | All in-Electron self-checks (16A–16F) and the WSL Parakeet live smoke are **host-run receipts** — this Linux sandbox verified internal consistency, timestamp-vs-commit ordering, and the falsification hash, but cannot execute them | — (disclosed trust boundary, same class as every prior tier) | One operator run of `node selfcheck/run.js assembled` on the host fully discharges |
| R4 | Non-hermetic test `test_probe_true_on_rc0` fails on hosts without `wsl` on PATH | INFO | Owed fix: monkeypatch `shutil.which` in the test; non-blocking |
| R5 | Nits: one in-place line rewrite in the append-only U-register (U68 State line); runner banner named the wrong report file (fixed in this commit → `FINAL_LIVE_REPORT.md`); an echoed iter-67 claim of a "sandbox-hook override" environment note was **never recorded** in LOOP_STATE (echo-vs-state gap; the untracked `.claude/settings.local.json` is benign MCP-enable config, no sandbox change) | INFO | Recorded here |

## Program state after this audit

Four completion tiers, each independently audited: `build/complete` (2026-07-18) →
`product/complete` (2026-07-19) → `product/live` (2026-07-24) → `product/usable` (this
audit). The operator's four first-use findings are addressed with in-Electron receipts:
panes stream and accept typing (16A), per-pane picker with live Ollama enumeration —
qwen3-class local, cloud Kimi honestly absent (16B), governed conductor pane 1 with
selection badge + dispatch (16C), real governor status bar + governed approvals + chrome-
carrying recovery (16D), voice end-to-end with **real WSL Parakeet proven on host** and
visibly-labelled mock fallback (16E), assembled 7-leg validation (16F). Honest OWED
surface: live interactive conductor backend (operator-run), U58 live worker legs, U70
live worker spawn from picker, mic-into-live-session, confidence calibration, U69
physical-keystroke chain, U73 pane:resize guard. Operator-reserved:
`SOVEREIGN_ORCHESTRATION_WORKSPACE_v1` promotion; TTS (I-V2 reversal); running the fully
live assembled system.
