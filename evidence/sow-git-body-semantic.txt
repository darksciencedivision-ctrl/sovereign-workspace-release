# utc: 2026-08-21T05:25:58.362457Z
# producer: claude-code REM-01
# purpose: classify the fc.exe /b difference between sow-git-before.body and sow-git-after.body

fc.exe /b reported differences. This file establishes WHY, field by field.

## 1. HEAD
  before : 6d23a81082836778ffd46c70151821b467dc7432
  after  : 6d23a81082836778ffd46c70151821b467dc7432
  EQUAL  : True

## 2. STATUS --porcelain=v1 -z entries (NUL-split, sorted)
  before : 3 entries
      M docs/loop/LOOP_STATE.json
     ?? docs/evidence/INDEPENDENT_REVIEW_WINDOWS_HOST_20260816.md
     ?? docs/evidence/PHASE19_UNIT10_U339_SUITE_AND_GATE.md
  after  : 3 entries
      M docs/loop/LOOP_STATE.json
     ?? docs/evidence/INDEPENDENT_REVIEW_WINDOWS_HOST_20260816.md
     ?? docs/evidence/PHASE19_UNIT10_U339_SUITE_AND_GATE.md
  EQUAL  : True

## 3. DIFF (binary) payload, from the first 'diff --git' line onward
  before : 12 lines, sha256=dcafc063dc67fe7f493919308c1349252e561493c13ec6adba826df25e3755db
  after  : 12 lines, sha256=dcafc063dc67fe7f493919308c1349252e561493c13ec6adba826df25e3755db
  EQUAL  : True

## 4. DIFF --cached payload
  before : 0 lines (empty means nothing staged)
  after  : 0 lines
  EQUAL  : True

## 5. Why the raw bytes differ
  before body line endings : CRLF
  after  body line endings : LF
  before body has UTF-8 BOM stripped by reconstruction: yes
  before section headers  : ### HEAD, ### STATUS --porcelain=v1 -z, ### STATUS --porcelain=v1 (human), ### DIFF, ### DIFF --cached, ### CLASSIFICATION
  after  section headers  : ### HEAD, ### STATUS --porcelain=v1 -z, ### DIFF --binary, ### DIFF --cached --binary
  before body contains PowerShell error-record text : True
  after  body contains PowerShell error-record text : False

## 6. Verdict
  GIT STATE IS UNCHANGED. HEAD, the porcelain status entries, the working-tree diff and
  the cached diff are all equal between the two captures.
  The fc.exe /b difference is entirely attributable to CAPTURE FORMAT:
    - CRLF line endings and a UTF-8 BOM in the Phase 0 capture vs LF raw bytes now
    - a 'STATUS --porcelain=v1 (human)' section and a 'CLASSIFICATION' section that
      exist only in the Phase 0 capture; R2-5 specifies four sections, not six
    - PowerShell NativeCommandError records interleaved into the Phase 0 DIFF section
      by a '2>&1 | Out-File' redirection, which is console noise, not git output
  Therefore PROTECTED_GIT_STATE_CHANGED is NOT raised: it would assert something false.
