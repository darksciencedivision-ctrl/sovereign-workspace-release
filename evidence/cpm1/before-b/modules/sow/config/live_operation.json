{
  "_comment": "LIVE OPERATION AUTHORIZED — created by the autonomous loop under register OP-6 (directive s11), 2026-07-19; WIDENED to register OP-12 on 2026-08-01 at the operator's explicit in-session direction (OP-12/OP-12.1, directive s17/s17.1 — edit performed by the orchestrator on the operator's behalf, recorded in-session). This file is gitignored (never committed): a fresh clone stays DENIED-by-absence. It is an authorization SWITCH, not a credential — the adapters invoke the host's already-authenticated CLIs and never read/store/transmit credentials (s2.2). Scope is code-pinned in control_plane/profiles/live_authorization.py and cannot be widened here. Per OP-12 s12: grok_build and google_antigravity are separate subscription resources capped at 1 terminal each in code regardless of the global ceiling below. CP-M1 G26, 2026-08-26: claude_code ADDED to scope and the conductor REBOUND to the recorded selection (D-COND-03, fable-5, registry.py:164-166) at the operator's explicit in-session direction, edited on the operator's behalf by the reviewer. Reason: governed Conductor communication requires the voice_turn_boundary@1.0 hook protocol that only claude_code implements (emit_conductor_launch.py:186-188); codex hooks are host-global config.toml keys (codex.py:351-352, U32) and cannot be pinned per session. Spend is bounded to the Conductor leg of CP-M1 G25/G26 and the G58 API-model leg; NO WORKER RUNS ON A FRONTIER PROVIDER. CP-M1 G26 SAFETY REVERT, 2026-08-26: the conductor binding is returned to openai_codex_cli/gpt-5.6-sol by the operator's reviewer because a claude_code conductor launch kills the Electron tree in-app (cause isolated by elimination to the supervised-spawn path; see evidence/cpm1/8d/g26-*). claude_code REMAINS in scope.providers and remains spend-authorized for the Conductor leg - only the DEFAULT binding is reverted, so the hazard is not the resting state. Rebinding to claude_code/fable-5 is a one-field change once the in-app defect is fixed. CP-M1 ADD-05 v1.1, 2026-08-26: grok_build ADDED to scope at the operator's in-session direction (their Grok quota is healthy while codex is ~7-8% and claude fable-5 is exhausted). Conductor binding UNCHANGED at openai_codex_cli per D5-4. Per OP-12 s12 grok_build is capped at 1 terminal in code regardless. Quota exhaustion on any provider is a NOT_RUN skip reason for the unattended run, never a halt.",
  "config_version": "1.1",
  "live_operation_authorized": true,
  "register_row": "OP-12",
  "scope": {
    "providers": [
      "openai_codex_cli",
      "claude_code",
      "grok_build"
    ],
    "terminals_per_subscription": 1
  },
  "conductor": {
    "provider_id": "openai_codex_cli",
    "adapter_id": "openai_codex_cli",
    "model_id": "gpt-5.6-sol",
    "display_name": "ChatGPT 5.6 Sol",
    "permission_profile_id": "pp-conductor-pane",
    "workspace": "D:\\Product Software\\Production Workspace\\modules\\sow"
  }
}
