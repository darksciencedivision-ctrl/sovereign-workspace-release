# utc: 08/25/2026 23:12:41.ToUniversalTime().ToString("o")
# producer: ox-alpha CP-M1 Band 4 / G26 launch-failure diagnosis

## What happened

POST /launch-conductor (driver 127.0.0.1:5199) returned
  {"launched":false,"reason":"Error: conductor-launch emitter emitted a malformed payload"}
captured verbatim: FACT[evidence/cpm1/8d/g26-launch-response.json].
Post-failure state captured: FACT[evidence/cpm1/8d/g26-state-2-postlaunchfail.json]
(launchState "unavailable" - the fail-closed path main.js:901-911).
Lease ledger after failure: subscriptions EMPTY - the shell released cleanly
FACT[evidence/cpm1/8d/g26-lease-status-postfail.json]. Zero spend; turn count unchanged (2).

## Why the message means what it says

FACT[apps/desktop/conductor/launch-source.js:301-312]: exit!=0 reports "emitter exited N";
unparseable stdout reports "emitted non-JSON"; ONLY exit-0 + valid JSON + isWellFormedTicket()
false produces "emitted a malformed payload". A governed REFUSAL ticket (authorized:false,
refused:true, reason) PASSES isWellFormedTicket (launch-source.js:188-189), so Python did not
refuse - Python emitted an AUTHORIZED claude_code ticket that the JS verifier rejects.

## Root cause (established statically + concretely, no further lease taken)

The voice-turn boundary's two sides disagree on how the hook command line is rendered:

- PRODUCER - FACT[node_runtime/supervisor/conductor_permission_profile.py:87-115] (unit W-38):
  render_hook_command() renders argv for PowerShell as SINGLE-quoted elements plus the call
  operator: & '<node>' '<hook.js>'  (_powershell_single_quote, :52-84; POWERSHELL_CALL_OPERATOR :27).
- VERIFIER - FACT[apps/desktop/conductor/launch-source.js:100-104]:
  b.hook_command_shell must be "powershell" (matches, :49) and then the validator RE-DERIVES the
  expected command as  arg.join(" ") with DOUBLE quotes around any element containing whitespace:
  const line = b.hook_argv.map((arg) => /\s/.test(arg) ? '"' + arg + '"' : arg).join(" ");
  const expectedCommand = HOOK_DISPATCH_SHELL === "powershell" ? "& " + line : line;
  if (b.hook_command !== expectedCommand) return false;
  The comment above it (:92-99) still describes subprocess.list2cmdline-era quoting - the
  pre-W-38 rendering. The validator was never updated to the single-quote contract.

Measured proof on THIS host (pure function only - no emitter run, no lease, no spend):
FACT[evidence/cpm1/8d/g26-hook-command-renderproof.txt]
  python_hook_command   : & 'D:\Program Files\nodejs\node.exe' 'D:\Product Software\...\voice_turn_boundary.js'
  js_expected_command   : & "D:\Program Files\nodejs\node.exe" "D:\Product Software\...\voice_turn_boundary.js"
  commands_equal        : false
Both real paths contain spaces ("Program Files", "Product Software"); the single-vs-double quote
difference makes the two strings unequal on EVERY argument of this repository layout, so
isWellFormedVoiceBoundary fails at launch-source.js:104 for every possible claude_code ticket here.

Every other checked field was verified consistent by reading both sides: enforced_by_supervisor_process
False required by JS :73 and produced by Python :212; prompt marker :74 / :22; seven hook events each
one command row :116-122 / :193-203; matchers ".*" :123 / :196-197; settings sha256 :111 / :223;
hook file sha256 :88 / :183; hook_runtime absolute existing node :80-82 / :173,:217; argv resolution
:89-91 / :184; lease shape :223-229. The hook_command rendering is the sole identified divergence.

## Why the suites stayed green

The JS-side unit suite validates isWellFormedVoiceBoundary against synthetic boundaries built
JS-side (apps/desktop/test/conductor-launch-source.test.js), so producer and verifier were never
round-tripped against each other in CI. The codex attempts never exercised this code path -
their tickets take isWellFormedFlagBoundary (launch-source.js:127-139), which contains no
hook_command check. This is the first claude_code ticket this build has ever tried to admit.

## Confidence statement (T-3)

The failing check is identified by reading both producers and reproducing the exact comparison
on the real values. What has NOT been done: executing the validator against a freshly minted real
ticket (each attempt mints a durable lease and starts spend). If an operator-authorized fix is
applied and ONE retry still fails, capture the next divergence the same way.

## Bounded operator options

1. AUTHORIZE THE SURGICAL VERIFIER FIX (recommended): align launch-source.js's expectedCommand
   derivation (:92-104) with the W-38 producer contract - derive via the same single-quote +
   call-operator rendering (_powershell_single_quote semantics), keep every other guard byte-for-
   byte. One file, small change, inside the Band 4 apps/desktop envelope, plus a unit-test pin
   round-tripping the REAL producer output against the validator (the missing seam). Then ONE
   authorized retry of POST /launch-conductor.
2. REVERT PYTHON W-38 to list2cmdline rendering so the old validator passes. RECOMMENDED AGAINST:
   it reinstates the measured U162 injection surface (a subexpression in an argv element executed
   as PowerShell code; measured in conductor_permission_profile.py:93-107).
3. RECORD the claude_code Conductor leg NOT_RUN(BLOCKED_VALIDATOR_DRIFT) and continue Band 5 with
   G28 + failure machinery only. Gate 8e becomes absences again (G29/G30 blocked; G31 split);
   R-04 stays unproven on BOTH providers.

No implementation was performed. The Electron tree is left RUNNING under operator ownership
(C-8): pids 70276/55440/78064/37608, driver healthy on 127.0.0.1:5199, launchState "unavailable",
lease ledger empty. The builder started no long-lived process this turn (C-8 compliant: only
seconds-lived py probes, all exited).
