# utc: 2026-08-25T22:24:45.512Z
# producer: ox-alpha CP-M1 Band 4 / G25 after-edit surface

## 1. Fails-before
See evidence/cpm1/8d/conductor-dom-fails-before.txt - the pre-edit renderer had NO
input element of any kind; that artifact lists every interactive element which
existed.

## 2. After: the persistent bar markup now shipped in renderer/index.html
The input carries NO disabled attribute; the submit button is a real form submit;
the transcript div is aria-live and retains turns across pane operations (turns
live in the main process, capped at 200, last 50 pushed to the renderer).

<style>/* conductor-bar tokens: #0d1117 bg, #c9d1d9 text, #79c0ff out-who, #7ee787 in-who */</style>
<div id="conductor-bar">
  <div id="conductor-transcript" aria-live="polite"></div>
  <form id="conductor-form">
    <input id="conductor-input" type="text" autocomplete="off"
           placeholder="Message the Conductor..." />
    <button id="conductor-send" type="submit">Send</button>
  </form>
</div>

## 3. Turn-row render contract (renderer.js wireConductorBar)
Each transcript turn renders as:
  <div class="turn out|in|sys">
    <span class="who">[YYYY-MM-DD HH:MM:SS] you:|Conductor:|system:</span>
    <span> text</span>
  </div>

## 4. Render-contract examples (structure only - NOT observed turns)
turn out example @ 22:20:00 on 2026-08-25: you: list the open approvals
turn in example @ 22:20:07 on 2026-08-25: Conductor: two approvals are pending.
turn sys example @ 22:20:08 on 2026-08-25: system: CONDUCTOR_COMMUNICATION_FAILED

## 5. Live DOM capture status
LIVE_INTERACTION_DEFERRED_TO_8J_STEP_D - headless Electron DOM dumping is not
available on this host; the timestamped live rows land in Band 10 scenario stepD,
which drives this exact surface against the running app.

