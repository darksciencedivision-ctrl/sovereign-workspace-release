# utc: 2026-08-26T05:46:14.7176750Z
# producer: ox-alpha CP-M1 Band 4 / G26 env isolation (S4/S5)

## Method (credential discipline)

One throwaway node process required the app's OWN modules (voice/turn-authority,
control/sovereign-control-server) and minted the child env EXACTLY as the launch path does:
authority.start() binding 127.0.0.1:0 as main.js:954, then authority.childEnv(baseEnv) per
main.js:967 [turn-authority.js:622-631], then control.childEnv(identity, env) per main.js:974-980
[sovereign-control-server.js:176-181]. Base = probe process.env minus the ticket's env_scrub_names
(empty here) - identical construction to scrubbedLaunchEnv for this ticket. Values existed only in
that process's memory; every artifact records NAMES and BYTE LENGTHS ONLY. No value was written to
any artifact, temp file or log.

## S4 - characterization   artifact: FACT[evidence/cpm1/8d/g26-s5-env-result.S4.json]

added over base: 7 variables -
  SOW_VOICE_AUTH_HOST    value 18 utf16 bytes (9 utf8)
  SOW_VOICE_AUTH_PORT            10            (5)
  SOW_VOICE_AUTH_TOKEN          128           (64)   <- largest; the 64-hex bearer token
  SOW_VOICE_AUTH_SCHEMA         64            (32)
  SOVEREIGN_CONTROL_PORT         0             (0)    <- see deviation note
  SOVEREIGN_CONTROL_TOKEN       86            (43)
  SOVEREIGN_STORE_ROOT         138            (69)
assembled block total: 8,292 bytes (UTF-16, separators and final terminator included).
flags: NONE - no value over 8 KB, total far below 32 KB, no NUL/newline/unpaired surrogate.
DEVIATION DISCLOSED: my SovereignControlServer instance never called start(), so its PORT value is
empty-string in this mint; in-app it would be a short port number. This cannot change any size or
format conclusion (the variable exists either way; its value would be ~2-5 bytes).

The oversized/malformed-environment-block hypothesis is DEAD on both legs: the block is ~8 KB of
short well-formed values, AND S5 below spawned successfully with exactly these variables present.

## S5 - spawn with that env      artifact: FACT[evidence/cpm1/8d/g26-s5-env-result.json]

outcome **(b)**: node survived; pty_pid 40608; 2,862 bytes of genuine Claude Code TUI received
(organization banner, cwd banner, 'Try "write a test for <filepath>"', auto-mode line); the pty
then exited on its own, as it did in S2. No bisect needed - nothing died. No lingering
.local\bin\claude.EXE child after the run (CIM scan empty; nothing killed).

## Verdict and recommendation (off-ramp taken)

The minted claude-branch env is NOT the fault: characterized benign (S4) and empirically harmless
in a real ConPTY spawn (S5). The isolation chain now reads:
  1. verifier drift        - found, fixed, pinned (round-trip test), suite-proven
  2. stale require cache   - diagnosed; resolved by operator restart under C-8(a)
  3. job-object kill       - REFUTED from source (H1)
  4. voice-authority/policy-bytes branch - REFUTED from source (H2)
  5. node-pty vs binary    - REFUTED empirically outside Electron (S2/H3)
  6. minted env            - REFUTED empirically with the real minted values (S4/S5)
REMAINING SUSPECTS (both inside Electron main, untestable without spending trees):
  a. SessionManager.spawn supervised admission wrapper around the pty spawn;
  b. the Electron main-thread/HTTP-driver context itself.
Per instruction, S5=(b) means I recommend THE OFF-RAMP: record G26 as NOT_RUN with cause
'CONDUCTOR_LAUNCH_PROCESS_DEATH - cause isolated by elimination to the in-app supervised-spawn
path; everything outside it exonerated by measurement', submit Gate 8d with the limitation stated
and this isolation chain as the finding, and proceed to Band 5's G28 plus the reachable failure
machinery (G31 machinery half).

## Spend

Zero. No conductor session, no provider turn; the CLI painted a first screen and self-exited.
