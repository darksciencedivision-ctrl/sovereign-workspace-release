# utc: 2026-08-26T04:43:23.6802675Z
# producer: ox-alpha CP-M1 Band 4 / G26 verifier repair - design-change record
# authorization: operator Option-1 decision this session (verbatim in OPERATOR-INSTRUCTIONS.log).

## Classification: REPAIR OF A DEFECT, NOT NEW SCOPE

The verifier (apps/desktop/conductor/launch-source.js isWellFormedVoiceBoundary) rejected its own
producer's valid output on any host whose paths contain whitespace; no claude_code conductor could
launch on this host. The repair re-unites the verifier with the W-38 producer contract. It changes
no governance semantics, relaxes no predicate, and touches neither the Python producer (permanently
closed against reverting), nor the argv vector, sandbox, model pin, or boundary schema.

## What changed

1. modules/sow/apps/desktop/conductor/launch-source.js
   - NEW powershellSingleQuoteArg(): algorithm-exact JS mirror of the producer's
     _powershell_single_quote (conductor_permission_profile.py:52-84) - BOTH layers:
     CommandLineToArgvW backslash-run doubling before an embedded escaped quote, then PowerShell
     single-quote wrap with apostrophes doubled.
   - isWellFormedVoiceBoundary: expectedCommand now RE-DERIVED from b.hook_argv through that mirror
     for the powershell branch (derive-don't-trust preserved); sh branch unchanged (off-host,
     per W-38 scope ruling). Stale list2cmdline comment replaced with the W-38 contract citation.
   - Apostrophe gap closed: literal ' in a path is HANDLED exactly as the producer handles it
     (doubling inside the single-quote wrap); literal " remains REFUSED explicitly (invalid in
     Windows paths; strictness on top of the mirror, never a substitute for it).
   - powershellSingleQuoteArg exported for the round-trip pin.
   Delta: +54/-5 = 59 changed lines (difflib unified vs fresh pre-edit capture
   evidence/cpm1/8d/before/launch-source.js sha256 15048cad648da34efffe09bf33e9f69a4bbebb00253ac9d165c3c8a9d202604d).
   sow-desktop 258 -> 317 of 700. TOTAL-A 371 -> 430 of 1850 (linecount-a.txt appended).

2. modules/sow/apps/desktop/test/conductor-hook-command-roundtrip.test.js (NEW, test file -
   cap-excluded): drives the REAL py renderer (_powershell_single_quote + render_hook_command)
   and the REAL JS mirror over the adversarial corpus named by the operator: spaces, apostrophe,
   apostrophe+spaces, dollar, backtick, semicolon, parenthesis, trailing backslash, two
   backslashes immediately before a quote, plus a plain control; element-level AND composed-line
   equality asserted; both-layers behavior pinned directly. 4 tests, 4 pass.

3. modules/sow/apps/desktop/test/conductor-launch-source.test.js (fixture refresh, test file):
   - synthetic voice boundaries now built through the SAME exported mirror instead of the stale
     double-quote-if-whitespace helper;
   - the live-integration expectation reads provider/model from config/live_operation.json
     instead of hardcoding openai_codex_cli/gpt-5.6-sol (the operator's Option C rebinding made
     the hardcode false; the authority is the config file).
   Suite result: node --test test/*.test.js -> 1108 tests, 1108 pass, 0 fail, exit 0
   (prior floor 1104 + this file's 4).

## Evidence chain

before: FACT[evidence/cpm1/8d/g26-hook-command-renderproof.txt] commands_equal false
after:  FACT[evidence/cpm1/8d/g26-hook-command-renderproof-after-fix.txt] commands_equal true
        (both REAL implementations driven; intermediate defective attempts disclosed in-file)
