# utc: 2026-08-26T02:43:43.3471074Z
# producer: ox-alpha CP-M1 Band 4 / G26 preflight P1
# purpose: operator instruction P1 - is claude_code available on this host? Detection ONLY.
# method: the adapter's own detection path, executed verbatim:
#   py -3.12 -c "from adapters.detect import claude_code_executable; print('detect.claude_code_executable() ->', repr(claude_code_executable()))"
#   cwd for that command: modules/sow (the adapters package root); PYTHONDONTWRITEBYTECODE=1

--- adapter detection output ---
detect.claude_code_executable() -> 'C:\\Users\\Sslaw\\.local\\bin\\claude.EXE'
--- claude --version (invoked as the resolved absolute path; operator-authorized one-shot version probe, no session, no spend) ---
stdout:
2.1.240 (Claude Code)
stderr:
exit: 0
