# utc: 2026-08-26T04:49:03.9016689Z
# producer: ox-alpha CP-M1 Band 4 / G26 retry-failure diagnosis (v2)

## What the authorized retry showed

POST /launch-conductor returned the SAME refusal as before the repair:
FACT[evidence/cpm1/8d/g26-launch-response-retry.json]
  {"launched":false,"reason":"Error: conductor-launch emitter emitted a malformed payload"}
Post-retry state: FACT[evidence/cpm1/8d/g26-state-4-postretry.json] (launchState "unavailable").
Host lease ledger afterwards: subscriptions EMPTY - nothing held, zero spend, count still 2.

## The repaired verifier is PROVEN correct against a REAL ticket

The emitter was run OUTSIDE the app against a SCRATCH lease ledger
(SOW_TERMINAL_LEASE_LEDGER override - the same isolation the selfchecks use; no durable state,
no spend), producing a full AUTHORIZED claude_code/fable-5 ticket:
FACT[evidence/cpm1/8d/g26-diag-ticket-scratch.nobom.json]
  argv[0] C:\Users\Sslaw\.local\bin\claude.EXE --model fable-5 --settings <pinned hook profile>;
  gates live_operation_authorized / operator_terms_confirmed / cli_present / ix3_counted all true;
  descriptor registered=true conductor_capable=true selection_source=live_operation_preference.
The REAL exported validator then judged that ticket:
FACT[evidence/cpm1/8d/g26-diag-ticket-validation.txt]
  isWellFormedTicket -> true, with every sub-predicate logged ok (schema, boundary fields,
  hook sha, settings sha, all seven hook events, derive-don't-trust command comparison, lease).

## Why the app still refuses: STALE MODULE CACHE in the operator-owned process

The Electron tree was started by the builder at 2026-08-26T03:24:30Z. The repair landed on disk
at 2026-08-26T04:24:27Z - one hour LATER. Node caches require()d modules for the life of the
process, so pid 70276 (and its children sharing the module registry) is still executing the OLD
launch-source.js with the pre-W-38 double-quote derivation. Evidence: electron start time
03:24:30Z (Get-Process) vs launch-source.js LastWriteTimeUtc 04:24:27Z.

The retry therefore tested the old code, not the repair. This is a consequence of C-8: the tree
is long-lived and operator-owned, and restarting it is an operator decision.

## What unblocks G26

ONE restart of the Electron tree so it loads the repaired launch-source.js, then the same
bounded driver sequence (GET /state -> POST /launch-conductor -> spend-log -> GET /state ->
round-trip turns). Under C-8 either (a) the operator restarts it and the builder attaches, or
(b) the operator authorizes the builder to start it fully detached per C-8(b): output to files
the OS owns, no held handles, poll-only.

## State at this stop

- Repair on disk, suite green (1108/1108, exit 0), renderproof-after commands_equal TRUE,
  real-ticket validation TRUE - all recorded.
- Zero spend; running turn count 2; host lease ledger empty.
- No third launch attempted (per instruction). Scratch diagnostic left no durable state.
