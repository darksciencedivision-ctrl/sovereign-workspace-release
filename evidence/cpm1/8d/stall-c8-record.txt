# utc: 08/25/2026 23:04:55.ToUniversalTime().ToString("o")
# producer: ox-alpha CP-M1 Band 4 / G26 stall finding + C-8 adoption

## The stall, recorded plainly

At 2026-08-26T03:24:30Z the builder spawned the SOW Electron app
(pid 70276, plus its child tree) using System.Diagnostics.Process with
RedirectStandardOutput=true / RedirectStandardError=true, intending to stream both into
evidence files via Register-ObjectEvent handlers. Electron is long-lived and never closes its
stdout; the builder's tool session blocked on the child's output handles and wrote nothing for
over thirty minutes. evidence/cpm1/8d/g26-electron-stdout.log holds five bytes (a BOM).
The conductor was NEVER launched - launchState stayed "unstarted"; no POST reached
/launch-conductor; zero spend occurred.

This is the third occurrence of this failure mode: the fs-watch helper stalled twice under
Start-Process with redirected handles (6h41m, then 59m), which produced C-4. C-4 was applied
literally to fs-watch only; this launch reproduced the identical mechanism with Electron.
The rule was never about fs-watch.

## Consequence - C-8 adopted as standing law

C-8 is now recorded in the amendment register
FACT[docs/SWS-UI-001-v1.2-ADDENDUM-04.md] section 2 table:
THE BUILDER NEVER SPAWNS A LONG-LIVED CHILD WITH REDIRECTED STDOUT OR STDERR. A long-lived
process is started either (a) by the operator, builder attaches, or (b) fully detached by the
builder, output to a file the OS owns that the builder never holds a handle to, then polled.
Handles only on processes that exit by themselves in seconds.
ADDENDUM-04 sha256 after this recording: 49dad6c2b2103ba5e5ab5dc47220e1ce579cda787c2ed3450b7a56c89f2c75d2

The same edit added the dated envelope-supersession note to ADDENDUM-04 section 6 recording the
operator's Option C decision (claude_code added to the authorized set), citing
docs/CP-M1-G26-CONFIG-AUTHORIZATION.md sha256 0c7d177b224135b199879367c24465a87d1e1a9095e79c6a3b3c9d717bf90177.

## Host state at re-attach (per operator verification, confirmed by the bounded GET below)

electron pids 70276, 55440, 78064, 37608 ALIVE since 03:24:30Z; driver 127.0.0.1:5199 listening;
launchState "unstarted"; alive false; transcript empty; spend count 2 (codex attempts of 01:22Z,
delivered=False). Per the operator instruction the tree is NOT killed and NOT relaunched - it is
the healthy driver this session now attaches to.

## Re-attach discipline

Every HTTP call from here is short-timeout and bounded (Invoke-WebRequest -TimeoutSec);
no new child processes; no held handles; every response captured to a FILE before being cited (T-1).
