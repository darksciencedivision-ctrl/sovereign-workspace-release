# utc: 2026-08-25T19:45:54.5789455Z # producer: ox-alpha CP-M1 Band 2 / G18 browser lifecycle  BEFORE (fails-before captured): shell/static/app.js:628 called   window.open(rec._url, "_blank", "noopener") and DISCARDED the returned handle. No tab was ever closed by the shell; orphan tabs accumulated across Start/Open cycles.  AFTER (this change):   const browserHandles = new Map();   // id -> live Window handle (app.js shared state)  Open action:   let h = browserHandles.get(id);   h = h && !h.closed ? h : window.open(rec._url, "_blank", "noopener");   if (h) browserHandles.set(id, h);   -> a second Open REUSES the live handle instead of spawning another tab.  applyModuleState:   leaving READY/EXTERNAL closes ONLY the shell-opened surface for that module:   closeBrowserHandle(id) runs before moduleState.set when the previous state was   READY/EXTERNAL and the new state is neither.  closeBrowserHandle(id): if (h && !h.closed) try { h.close() }; map entry deleted. Guarantees: no taskkill / process.kill / Stop-Process anywhere in app.js (pinned by test_no_process_kills_against_browsers); the shell can never touch a browser window it did not open (H-9 spirit at the renderer layer).  Proofs: test-browser-handle-map-fails-before.txt (3 failures pre-change), test-browser-handle-map-passes-after.txt (4/4 OK post-change; test_frontend 5/5 OK); pinned structurally in shell/tests/test_browser_handle_map.py.
