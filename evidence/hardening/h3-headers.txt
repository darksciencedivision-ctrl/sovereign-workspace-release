# utc: 2026-08-27T05:04:01Z
# producer: claude-code REM-01
# proof: H-3 - required security headers on every response

### curl.exe -si http://127.0.0.1:56155/
HTTP/1.1 200 OK
Server: BaseHTTP/0.6 Python/3.14.6
Date: Thu, 27 Aug 2026 05:04:01 GMT
Content-Type: text/html; charset=utf-8
Content-Length: 3217
X-Content-Type-Options: nosniff
Referrer-Policy: no-referrer
Permissions-Policy: camera=(), microphone=(), geolocation=()
Cache-Control: no-store
Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self'; connect-src 'self'; img-src 'self' data:; frame-ancestors 'none'; object-src 'none'; base-uri 'none'

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="color-scheme" content="dark light">
  <meta name="csrf-nonce" content="6e0c0caee37fee8675362c9a557c73d245c6788439251ffb98134dd43f577d52">
  <title>Sovereign Workspace Shell</title>
  <link rel="stylesheet" href="/static/app.css">
  <script src="/static/app.js" defer></script>
</head>
<body>
  <div class="shell">

    <!-- Header bar: SWS version, build ID, host, clock, document links -->
    <header class="header">
      <div class="header-brand">
        <span class="header-title">SWS</span>
        <span class="header-ver" id="shell-version">v â€”</span>
        <span class="header-sep" aria-hidden="true">Â·</span>
        <span class="header-build" id="shell-build">build â€”</span>
        <span class="header-sep" aria-hidden="true">Â·</span>
        <span class="header-host" id="shell-host">host â€”</span>
      </div>
      <div class="header-right">
        <nav class="header-nav" aria-label="Shell documents">
          <a class="header-link" id="link-discovery" href="/doc/discovery">DISCOVERY</a>
          <a class="header-link" id="link-theme-baseline" href="/doc/theme-baseline">THEME-BASELINE</a>
          <a class="header-link" id="link-directive" href="/doc/directive">directive</a>
        </nav>
        <time class="header-clock" id="shell-clock">--:--:--</time>
      </div>
    </header>

    <!-- Pre-flight panel (collapsible): populated by app.js -->
    <details class="panel preflight-panel" id="preflight-details" open>
      <summary class="panel-summary">
        <span class="summary-title">Pre-flight</span>
        <span class="summary-note" id="preflight-summary">checkingâ€¦</span>
      </summary>
      <ul class="preflight-grid" id="preflight-grid" aria-label="Pre-flight checks"></ul>
    </details>

    <!-- Module grid: 4 cards in fixed order, built by app.js -->
    <main class="module-grid" id="module-grid" aria-label="Workspace modules"></main>

    <!-- Log pane (collapsible): log output for selected module -->
    <details class="panel log-panel" id="log-details">
      <summary class="panel-summary">
        <span class="summary-title">Logs</span>
        <span class="summary-note" id="log-module-name">no module selected</span>
      </summary>
      <div class="log-toolbar">
        <span class="log-status" id="log-status" role="status"></span>
        <button class="btn btn-small" id="log-close" type="button">Close (Esc)</button>
      </div>
      <pre class="log-output" id="log-output" role="log" aria-live="polite">No module selected. Use a moduleâ€™s Logs button.</pre>
    </details>

    <footer class="footer">
      <span>SWS-UI-001 v1.2 â€” Sovereign Workspace Shell</span>
      <span class="footer-note">local dashboard Â· no external assets</span>
    </footer>

    <!-- Live region for action feedback (screen readers) -->
    <p class="visually-hidden" id="action-status" role="status" aria-live="polite"></p>

    <noscript>
      <p class="noscript">The Sovereign Workspace Shell requires JavaScript to query and control modules.</p>
    </noscript>
  </div>
</body>
</html>

### curl.exe -si http://127.0.0.1:56155/api/state
HTTP/1.1 200 OK
Server: BaseHTTP/0.6 Python/3.14.6
Date: Thu, 27 Aug 2026 05:04:01 GMT
Content-Type: application/json
Content-Length: 1915
X-Content-Type-Options: nosniff
Referrer-Policy: no-referrer
Permissions-Policy: camera=(), microphone=(), geolocation=()
Cache-Control: no-store
Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self'; connect-src 'self'; img-src 'self' data:; frame-ancestors 'none'; object-src 'none'; base-uri 'none'

{"modules": {"debate": {"id": "debate", "display_name": "Debate Table", "description": "Multi-model debate table (FastAPI, :8700)", "state_class": "runnable", "state": "STOPPED", "reason": "", "display": "STOPPED", "last_check": "2026-08-27T05:03:59Z", "port": "", "url": "http://127.0.0.1:8700/", "can_open": false}, "distillery": {"id": "distillery", "display_name": "Sovereign Distillery", "description": "Distillery runtime console \u2014 health only, no compute on open", "state_class": "runnable", "state": "STOPPED", "reason": "", "display": "STOPPED", "last_check": "2026-08-27T05:03:59Z", "port": "", "url": "http://127.0.0.1:5184/console", "can_open": false}, "llamacpp": {"id": "llamacpp", "display_name": "llama.cpp (candidate)", "description": "Pinned llama.cpp router on 5183. Not production default.", "state_class": "runnable", "state": "STOPPED", "reason": "", "display": "STOPPED", "last_check": "2026-08-27T05:03:59Z", "port": "", "url": "", "can_open": false}, "sovereign": {"id": "sovereign", "display_name": "SOVEREIGN", "description": "Operator product service and UI (Flask, :5175)", "state_class": "runnable", "state": "STOPPED", "reason": "", "display": "STOPPED", "last_check": "2026-08-27T05:03:59Z", "port": "", "url": "http://127.0.0.1:5175/", "can_open": false}, "sow": {"id": "sow", "display_name": "Multi-Model Terminal", "description": "Sovereign Orchestration Workspace (Electron + xterm.js)", "state_class": "runnable", "state": "STOPPED", "reason": "", "display": "STOPPED", "last_check": "2026-08-27T05:03:59Z", "port": "", "url": "", "can_open": false}, "tokencenter": {"id": "tokencenter", "display_name": "Token Center", "description": "Read-only local usage telemetry. No credentials.", "state_class": "runnable", "state": "STOPPED", "reason": "", "display": "STOPPED", "last_check": "2026-08-27T05:03:59Z", "port": "", "url": "http://127.0.0.1:8765/", "can_open": false}}}
### curl.exe -si http://127.0.0.1:56155/static/app.css
HTTP/1.1 200 OK
Server: BaseHTTP/0.6 Python/3.14.6
Date: Thu, 27 Aug 2026 05:04:01 GMT
Content-Type: text/css
Content-Length: 8933
X-Content-Type-Options: nosniff
Referrer-Policy: no-referrer
Permissions-Policy: camera=(), microphone=(), geolocation=()
Cache-Control: no-store
Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self'; connect-src 'self'; img-src 'self' data:; frame-ancestors 'none'; object-src 'none'; base-uri 'none'

/* ============================================================
   Sovereign Workspace Shell â€” app.css (SWS-UI-001 v1.2)
   - Theme tokens as CSS custom properties on :root
   - No gradients, no glow; box-shadows avoided entirely
     (focus uses outline instead)
   - Spacing scale: 4 / 8 / 12 / 16 / 24 px
   - Transitions <= 150 ms
   - Contrast (dark): --text #c7d1de on --bg #0b0e14  ~ 12.5:1
                      --text #c7d1de on --panel #151b28 ~ 11.2:1
     Contrast (light): --text #1b2027 on --bg #f4f5f7  ~ 14.9:1
                       --text #1b2027 on --panel #ffffff ~ 16.3:1
   ============================================================ */

:root {
  --bg: #0b0e14;
  --panel: #151b28;
  --sidebar: #0d121c;
  --input: #1b2333;
  --border: #232b3a;
  --text: #c7d1de;
  --text-muted: #9aa7bd;
  --text-faint: #3a4353;
  --accent: #7fc8e8;
  --accent-dim: #2a4a5c;
  --ok: #9ece6a;
  --warning: #e0af68;
  --danger: #f7768e;
  --radius: 6px;
  --font: ui-monospace, monospace;
  --mono: "Cascadia Code", Consolas, monospace;
}

@media (prefers-color-scheme: light) {
  :root {
    --bg: #f4f5f7;
    --panel: #ffffff;
    --sidebar: #eceef1;
    --border: #d3d8e0;
    --text: #1b2027;
    --text-muted: #58616e;
    --text-faint: #8a93a1;
    --accent: #1f6f94;
    --accent-dim: #a8d6ea;
    --ok: #2f7a49;
    --warning: #8a6008;
    --danger: #c8394f;
  }
}

/* ---------- reset ---------- */

*,
*::before,
*::after {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  padding: 0;
}

body {
  background: var(--bg);
  color: var(--text);
  font-family: var(--font);
  font-size: 14px;
  line-height: 1.5;
}

/* ---------- layout ---------- */

.shell {
  max-width: 1200px;
  margin: 0 auto;
  padding: 24px 16px;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

/* ---------- header ---------- */

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 16px;
  flex-wrap: wrap;
  background: var(--sidebar);
  border: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 12px 16px;
}

.header-brand {
  display: flex;
  align-items: baseline;
  gap: 8px;
  flex-wrap: wrap;
}

.header-title {
  font-weight: 600;
  letter-spacing: 0.08em;
  color: var(--accent);
}

.header-ver,
.header-build,
.header-host {
  color: var(--text-muted);
  font-size: 12px;
  font-family: var(--mono);
}

.header-sep {
  color: var(--text-faint);
}

.header-right {
  display: flex;
  align-items: center;
  gap: 16px;
  flex-wrap: wrap;
}

.header-nav {
  display: flex;
  gap: 12px;
}

.header-link {
  color: var(--text-muted);
  text-decoration: none;
  font-size: 12px;
  letter-spacing: 0.06em;
  padding: 4px 0;
  border-bottom: 1px solid transparent;
  transition: color 120ms ease, border-color 120ms ease;
}

.header-link:hover {
  color: var(--accent);
  border-bottom-color: var(--accent-dim);
}

.header-link:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
  border-radius: 2px;
}

.header-clock {
  font-family: var(--mono);
  color: var(--text-muted);
  font-size: 13px;
}

/* ---------- collapsible panels ---------- */

.panel {
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: var(--radius);
}

.panel-summary {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 12px 16px;
  cursor: pointer;
  user-select: none;
}

.panel-summary::marker {
  color: var(--accent);
}

.panel-summary:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: -2px;
  border-radius: var(--radius);
}

.summary-title {
  font-weight: 600;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  font-size: 12px;
  color: var(--text-muted);
}

.summary-note {
  margin-left: auto;
  color: var(--text-faint);
  font-size: 12px;
  font-family: var(--mono);
}

/* ---------- pre-flight ---------- */

.preflight-grid {
  list-style: none;
  margin: 0;
  padding: 4px 16px 16px;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
  gap: 8px 16px;
}

.preflight-item {
  display: flex;
  align-items: center;
  gap: 8px;
  min-width: 0;
}

.preflight-label {
  color: var(--text);
  font-size: 13px;
  white-space: nowrap;
}

.preflight-detail {
  margin-left: auto;
  color: var(--text-faint);
  font-family: var(--mono);
  font-size: 12px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* ---------- dots & badges ---------- */

.dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--text-faint);
  flex: 0 0 auto;
  transition: background-color 120ms ease;
}

.dot-ok { background: var(--ok); }
.dot-bad { background: var(--danger); }
.dot-unknown { background: var(--warning); }

.badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-size: 13px;
  font-weight: 600;
}

.badge .dot {
  background: currentColor;
}

.badge-ok { color: var(--ok); }
.badge-warning { color: var(--warning); }
.badge-danger { color: var(--danger); }
.badge-muted { color: var(--text-muted); }
.badge-external { color: var(--warning); }

/* ---------- module grid & cards ---------- */

.module-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
}
.module-card[data-module="tokencenter"] {
  grid-column: 1 / -1;
}

.module-card {
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.module-name {
  margin: 0;
  font-size: 16px;
  font-weight: 600;
  letter-spacing: 0.02em;
}

.module-desc {
  margin: 0;
  color: var(--text-muted);
  font-size: 13px;
}

.state-line {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
}

.state-reason {
  color: var(--text-muted);
  font-size: 12px;
}

.module-meta {
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.meta-row {
  display: flex;
  gap: 8px;
  font-size: 12px;
}

.meta-label {
  color: var(--text-faint);
  flex: 0 0 100px;
}

.meta-value {
  margin: 0;
  color: var(--text-muted);
  font-family: var(--mono);
  overflow-wrap: anywhere;
}

/* ---------- distillery extras ---------- */

.distillery-links {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
  font-size: 12px;
}

.distillery-link {
  color: var(--accent);
  text-decoration: none;
  border-bottom: 1px solid var(--accent-dim);
  padding-bottom: 1px;
  transition: color 120ms ease;
}

.distillery-link:hover {
  color: var(--text);
}

.distillery-link:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

.distillery-nolinks {
  color: var(--text-faint);
}

.distillery-note {
  margin: 0;
  color: var(--text-faint);
  font-size: 12px;
  font-style: italic;
  border-left: 2px solid var(--border);
  padding-left: 8px;
}

/* ---------- buttons ---------- */

.actions {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: auto;
  padding-top: 4px;
}

.btn {
  appearance: none;
  border: 1px solid var(--accent-dim);
  border-radius: var(--radius);
  background: var(--accent);
  color: var(--bg);
  font-family: var(--font);
  font-size: 13px;
  font-weight: 600;
  padding: 8px 12px;
  cursor: pointer;
  transition: background-color 120ms ease, border-color 120ms ease;
}

.btn:hover:not(:disabled) {
  background: var(--accent-dim);
}

.btn:active:not(:disabled) {
  background: var(--accent-dim);
}

.btn:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

.btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.btn-small {
  padding: 4px 8px;
  font-size: 12px;
}

/* ---------- log pane ---------- */

.log-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  padding: 4px 16px 12px;
}

.log-status {
  color: var(--text-faint);
  font-size: 12px;
  font-family: var(--mono);
}

.log-output {
  margin: 0;
  background: var(--panel);
  color: var(--text);
  font-family: var(--mono);
  font-size: 12px;
  line-height: 1.5;
  white-space: pre-wrap;
  word-break: break-word;
  max-height: 320px;
  overflow-y: auto;
  padding: 16px;
  border-top: 1px solid var(--border);
  border-radius: 0 0 var(--radius) var(--radius);
}

/* ---------- footer & misc ---------- */

.footer {
  display: flex;
  justify-content: space-between;
  gap: 8px;
  flex-wrap: wrap;
  color: var(--text-faint);
  font-size: 12px;
}

.noscript {
  color: var(--danger);
  margin: 0;
}

.visually-hidden {
  position: absolute;
  width: 1px;
  height: 1px;
  margin: -1px;
  padding: 0;
  overflow: hidden;
  clip: rect(0 0 0 0);
  white-space: nowrap;
  border: 0;
}

/* ---------- responsive ---------- */

@media (max-width: 720px) {
  .module-grid {
    grid-template-columns: 1fr;
  }

  .header {
    flex-direction: column;
    align-items: flex-start;
    gap: 8px;
  }

  .meta-label {
    flex-basis: 88px;
  }
}
