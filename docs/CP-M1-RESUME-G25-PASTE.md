OPERATOR INSTRUCTION — CP-M1 resume at G25. One correction (E-6), then Band 4, which is the first goal in this package that spends money.

Reviewer verified on disk: ledger 7f3c6d88..43d744, fifteen keys, gates 8a/8b/8c CANDIDATE, gates 0-7b full-object identical to the verified snapshot with zero drifting fields. Guard confirmed widened AND relocated - ledger_snapshot_ok() now runs at goalcheck.py:1424 before the FUNCS loop and short-circuits to "GUARD-FAIL - no goal evaluated", with g124's hardcoded map replaced. Provenance header present and correctly worded. Gap-15 account, regression account, E-5 qualifier all present. G22 verified in source: worker-spawn.js:244 requires selection.replaceConfirmed === true and calls the injected endLiveSession(pid), which main.js:2065 binds to manager.kill - the same primitive pane-close uses. All 19 of gate 8c's evidence hashes recomputed and MATCH. Suite Ran 140 OK. TOTAL-A 232/1850. Band 3 accepted.

E-6 - Gate 8c cites a stale oracle run. Its evidence roster includes evidence/cpm1/goalcheck-9.txt, which is 19:46Z, predates Band 3 entirely, and reads "summary: 16/124 TRUE". The hash is accurate; the citation is not. A gate must cite the oracle run that witnessed its own goals TRUE, not an earlier one. Re-point 8c to the run that first shows G20-G23 TRUE, and check gates 8a and 8b for the same defect - if either cites an oracle run predating its own band, correct it the same way and say so. Ledger edit only; no gate changes status, gates 0-7b stay byte-identical, and the guard must pass before and after.

Then G24 stands and you proceed to Band 4.

BAND 4 IS THE SPEND BOUNDARY. READ THIS BEFORE G26.

A-6 BINDS AND REPLACES G26 AS WRITTEN. The work order at docs/OX-ALPHA-DIRECTIVE-CP-M1.md line 167 says the Conductor leg "runs on a local model - no spend is authorized." That predicate is UNSATISFIABLE and the file was deliberately left unamended so the amendment stays traceable. CONDUCTOR_MODEL_REGISTRY in modules/sow/control_plane/conductor/registry.py holds exactly three entries - openai_codex_cli/gpt-5.6-sol, claude_code/fable-5, claude_code/opus-4.8 - all locality "frontier". There is no local conductor-capable model. Do not attempt to satisfy the local predicate, do not register a new local conductor entry (that is new scope and CP-M1 s9 forbids it), and do not STOP on it. G26 reads instead:

  G26 - Round-trip: human -> Conductor -> human. evidence/cpm1/8d/conductor-roundtrip.txt: a directive typed into the surface; the Conductor's response returned into the transcript; a follow-up in the same thread demonstrably carrying prior context; and an interrupt or redirect exercised, or recorded UNSUPPORTED(<reason>). The Conductor leg runs on whichever conductor-capable model the operator's spend authorization permits - openai_codex_cli/gpt-5.6-sol - recorded by name and provider in the artifact. A Conductor communication failure surfaces as CONDUCTOR_COMMUNICATION_FAILED (G17), not as silence.

SPEND ENVELOPE, exact and bounded. Authorized: openai_codex_cli only, for (a) the Conductor leg of G25/G26 and (b) the API-model leg of the G58 proof chain. Nothing else. claude_code, grok_build and google_antigravity legs record NOT_RUN(NO_SPEND_AUTHORIZATION) with the registry entries examined. terminals_per_subscription stays 1. No worker runs on a frontier provider.

Write evidence/cpm1/8d/spend-log.txt as you go: every Conductor turn, timestamped, with the model and provider named, and a running turn count. Keep the round-trip minimal - it is a proof of communication, not a working session. If G26 needs more than roughly a dozen Conductor turns to demonstrate the round-trip, stop and report rather than continuing to spend; that is a design problem in the surface, not a budget problem. Any spend outside the envelope above is STOP SPEND_REQUIRED with no interpretation permitted.

Then run the ladder: G25 (persistent typing surface - note the fails-before capture is mandatory, the current renderer has no input element at all), G26 (round-trip under A-6), G27 (Gate 8d), and on through Band 5 and beyond without pausing at band boundaries until every goal is TRUE or a STOP fires.

A-7: same OpenCode session confirmed by the reviewer - the 16:30Z fs-watch window stays open, you record nothing, you do not touch the driver. C-4 stands.

Unchanged and still binding: builder only - no PASS, no operator signature, no protected-source writes, no fabricated availability, no production promotion. Gates 0-7b byte-identical before and after every write, now enforced by the preamble guard. Ollama's store is read-only always. llama.cpp is not promoted to production default. Application code never launches via cmd.exe, PowerShell, .cmd, .bat or shell=True.
