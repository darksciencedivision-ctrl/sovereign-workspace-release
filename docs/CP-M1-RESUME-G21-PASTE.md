OPERATOR INSTRUCTION — CP-M1 resume at G21, with corrections E-1..E-5.

The reviewer verified D-1..D-4 independently on disk. All four are confirmed: ledger sha256 e5ae4224..54d9ad, keys [0,1,2,3,4,5,6,4b,4c,5b,7a,7b,8a,8b], gate 4 = 659ff9b635a22ddbdc9065c56ac5fbb277dcce25d3110354acb5e9134fed9f6d (full 64, "dc" present), gate 5 = df4cc05394c0a7756ea4e48666661cd8041686dc712ee54931bd0d73fb2c07d7, both witnesses agree, 406 evidence rows compared across both snapshots with zero mismatches, no malformed hash anywhere in the ledger, reviewer attribution intact, audit 235 data rows. The only entry carrying the live hash dca3eb40.. is gate 8b, legitimately. Your gate-4 audit line recorded a MISMATCH despite identical 12-character display prefixes; that honesty is why the second hit was caught. Read docs/CP-M1-VERIFY-01.md before acting.

Five corrections. E-2 and E-3 are guard defects and come FIRST, before any further ledger write. Log this instruction verbatim with UTC into evidence/OPERATOR-INSTRUCTIONS.log before any other mutation, then:

E-2 - Widen the guard from two fields to the whole entry. ledger_snapshot_ok() compares status and the evidence set only. It does not compare evaluated_by, reviewer_note, utc, basis or superseded_ledger_sha256 - so a write that flipped evaluated_by from reviewer to builder passes it silently, and "never alter reviewer-evaluated entries" is a standing constraint of this package. Compare the FULL gate object for all twelve keys 0,1,2,3,4,4b,4c,5,5b,6,7a,7b. The reviewer already confirmed every field is currently identical to the snapshot, so this costs zero repairs today. Report the diff-field list the widened guard produces on its first run.

E-3 - Move the guard off one predicate. ledger_snapshot_ok() is called from exactly one site, goalcheck.py:541 inside g19(). Its coverage is incidental. Call it from the per-iteration preamble so it gates every ledger write, and replace g124()'s hardcoded status map with the same call. Log this as goal G-oracle.

E-1 - Stop overstating gates 7a and 7b. Neither witness contains a 7a or 7b key; both snapshots predate THEME-01. ledger-snapshot-verified.json now freezes those two at values no independent artifact ever attested. Their BUILD-MANIFEST hash 6ebc420e..5235e is not the live hash, which is evidence of absence of THIS fault, not evidence of correctness. Add a provenance header to ledger-snapshot-verified.json naming witness-corroborated keys (0,1,2,3,4,4b,4c,5,5b,6) separately from self-attested keys (7a,7b), and state it in the gate note. Restore nothing.

E-4 - Account for the hole at LOOP-LEDGER index 15. Sixteen lines, indices 1..14,16,17. Your "16 ledger lines" is true; the sequence is not contiguous, and an append-only ledger with a gap cannot distinguish a skipped counter from a removed entry. State in one line what happened. Do NOT renumber and do NOT synthesise a line 15.

E-5 - Gate 8b's note says TOTAL-A 129+test files; linecount-a.txt now reads TOTAL-A 166/1850 because G20 added 37 lines after the note was written. Qualify the figure as "as of Band 2 close" or amend it. Note edit only; the gate does not change.

Also account, one line each, for the two oracle-count regressions: goalcheck-8 17/124 -> goalcheck-9 16/124, and goalcheck-13 21/124 -> goalcheck-14 20/124. Name the goal that went FALSE and why in each case. A goal-state count that moves backwards without a ledger line is the shape of the defect class this package exists to catch.

FS-WATCH, A-7: you paused, you did not end your session. If this is the same OpenCode session, the window opened at 16:30 STAYS OPEN - one continuous window from G5 is stronger evidence than two with a gap - and you record nothing. If this is a NEW session, the operator closes the old window and opens a fresh one; you record the gap in the A-7 table and you do not start the driver yourself (C-4 stands: the operator owns the fs-watch helper, and Start-Process with redirected handles is what stalled you twice).

THEN RESUME THE LOOP AT G21 - selection flow while EMPTY/CONFIGURING - and run without pausing at band boundaries until every goal is TRUE or a STOP fires. G22 next (governed replacement of the "already holds a live session" refusal), then G23 (powershell-not-default grep-proof), then G24 (Gate 8c).

Unchanged and still binding: builder only - no PASS, no operator signature, no protected-source writes, no fabricated availability, no production promotion. No provider spend beyond openai_codex_cli for the G25/G26 Conductor leg and the G58 API-model leg; every other provider leg records NOT_RUN(NO_SPEND_AUTHORIZATION). Gates 0-7b byte-identical before and after every write. Ollama's store is read-only always. llama.cpp is not promoted to production default. Application code never launches via cmd.exe, PowerShell, .cmd, .bat or shell=True.
