# Root attributes — CLOSEOUT-01.
# F-074: text files checkout as LF regardless of core.autocrlf, so hash gates
# that read working-tree bytes match git blobs and git archive. Subtree
# modules/sow/.gitattributes and modules/distillery/.gitattributes already set
# eol=lf; this extends the same rule to shell/, tools/, and root manifests.
# export-ignore rules below still apply unopposed (deeper files do not set them).
#
# export-ignore removes a path from `git archive` ONLY. Every path below stays
# tracked, stays in `git ls-files`, and stays in history.

* text=auto eol=lf
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.bundle binary
*.whl binary
*.pyc binary
*.pyd binary
*.dll binary
*.exe binary
*.woff binary
*.woff2 binary
*.ttf binary
*.otf binary
*.sqlite binary
*.db binary
*.pdf binary

# X-2 (A-2 / OD-25) — the Phase-1 throwaway spike is not product code. Its
# package-lock pins electron 31.7.7 and extract-zip 2.0.1, the two
# high-severity findings. No product file imports it; the three product
# references to it are prose comments saying it is NOT the real component.
# Excluding it closes C2-RISK-SPIKE-ELECTRON-LOCK-HELD-NONPRODUCT mechanically
# instead of shipping a vulnerable dependency declaration.
# Both forms are required. The `/**` form excludes the CONTENTS; the bare
# directory form excludes the directory ENTRY itself. With only `/**`, git
# archive still emits two empty directory members - measured, not assumed.
modules/sow/tools/spike_compositor export-ignore
modules/sow/tools/spike_compositor/** export-ignore
evidence/cpm1/before-b/modules/sow/tools/spike_compositor export-ignore
evidence/cpm1/before-b/modules/sow/tools/spike_compositor/** export-ignore

# X-3 (A-3 / OD-26) — backup snapshots stay tracked as history but do not ship.
# A distribution carrying two contradicting SBOMs and two contradicting release
# manifests gives a consumer or scanner no rule for which is authoritative.
# Preserving prior records is right; shipping them is not.
#
# Nothing reads these at runtime or during install, verified before excluding:
#   rebase_adapters.py:151-153 tests `backup.exists()` and then WRITES a copy;
#     it never reads a backup's contents. Absent backups are simply recreated.
#   verify_install.ps1 builds its expected path set from the install-time
#     install-manifest.json, not from any committed list, so build and verify
#     stay consistent with each other.
*.pre-CONVERGE01 export-ignore
*.pre-rebase export-ignore

# The directive names the pattern `*.pre-add08`. That pattern matches NOTHING:
# the sole file is evidence/cpm1/baseline/host-hardware.pre-add08.json, where
# `.pre-add08` is an INFIX, not the terminal suffix. Both forms are listed —
# the exact one that matches today, and the suffix form for any future file
# that does end in `.pre-add08`.
*.pre-add08 export-ignore
*.pre-add08.json export-ignore

# WS-0.2 clean-room boot gate — version-controlled tooling, not part of the shipped artifact.
tools/cleanroom export-ignore
tools/cleanroom/** export-ignore

# WS-2.3 — build-control / dev-evidence artifacts DO NOT SHIP. Heavy, non-runtime records used
# to govern the autonomous build; kept tracked and in history, excluded from `git archive`.
# Verified not loaded by any runtime code (only referenced in a source comment).
modules/sow/docs/registers export-ignore
modules/sow/docs/registers/** export-ignore
modules/sow/docs/loop export-ignore
modules/sow/docs/loop/** export-ignore
modules/sow/tools/soak/results export-ignore
modules/sow/tools/soak/results/** export-ignore
modules/distillery/runs/export export-ignore
modules/distillery/runs/export/** export-ignore

# X-4 (OD-35) — THE DEVELOPMENT RECORD DOES NOT SHIP. Operator decision,
# 2026-08-30, taken on the STATE-OF-SYSTEM/PUNCH-LIST V6 measurements of
# ENTRY 020. The root evidence/ and dev/ trees are 1931 tracked files at the
# seal and roughly 76% of the archive's bytes. Cutting them from the
# distribution:
#   - removes evidence/cpm1/before-b/modules/sow/config/live_operation.json,
#     the POPULATED authorization switch of N-40. That file's own _comment
#     asserts "This file is gitignored (never committed): a fresh clone stays
#     DENIED-by-absence" — an assertion this repository falsifies, because the
#     file was committed as a checkpoint snapshot. Removing it from the archive
#     makes the assertion true again for a recipient.
#   - removes the developer username and the personal directory tree from the
#     103 and 255 files respectively that carry them.
# History stays tracked; only `git archive` output changes. Nothing in the
# product reads either tree — verified before excluding, over all 1154 tracked
# code files outside them: every runtime path reference is to
# `docs/evidence/...` under modules/sow, which is a DIFFERENT tree and is not
# matched here.
#
# ANCHORING IS LOAD-BEARING. A gitattributes pattern containing no slash is
# matched at every directory depth, so a bare `evidence` would also take
# docs/evidence/ (1 file) and modules/sow/docs/evidence/ (250 files) — the
# receipt tree that ~40 product source files cite by path. The leading slash
# anchors each rule to this file's directory, the repository root. As with X-2,
# both forms are required: the `/**` form excludes the CONTENTS, the bare
# directory form excludes the directory ENTRY itself.
#
# X-2's two evidence/cpm1/before-b/... rules above are now redundant — this
# rule subsumes them. They are left in place rather than deleted so that the
# X-2 record stays legible on its own terms.
/evidence export-ignore
/evidence/** export-ignore
/dev export-ignore
/dev/** export-ignore

# X-5 (EPC-01 P3-3) — THE WORKSHOP'S PAPERWORK DOES NOT SHIP EITHER.
#
# OD-35 removed evidence/ and dev/ from the distribution. It did not touch docs/, which ships
# 90 markdown files of which 58 are internal build directives, review pastes, resume pastes,
# conflict audits and stop reports — the same class of artifact, in a directory OD-35 did not
# reach. A recipient opening docs/ should find the documents written FOR them, not the running
# commentary of the build that produced their copy.
#
# tools-operator/ goes with them and for the same reason: its six Start-OpenCode-*.ps1 files
# are build-harness launchers, they are the only things that reference the cut documents, and
# no product file references THEM (measured across shell/, modules/ and tools/).
#
# What deliberately stays: every ADR, DECISIONS.md, DISCOVERY.md, THEME-BASELINE*, the
# SWS-UI-001 addenda, RELEASE-ASSURANCE.md, LIMITATIONS.md, SUPPORT-POLICY.md and
# THIRD-PARTY-LICENCE-POSITION.md. The README links four of those, and a recipient needs the
# architecture decisions to understand what they are running.
#
# History stays tracked; only `git archive` output changes.
docs/CP-* export-ignore
docs/REM-0* export-ignore
docs/REVIEW-* export-ignore
docs/STOP-* export-ignore
docs/OX-* export-ignore
docs/EXPORT-* export-ignore
docs/CLAUDE-* export-ignore
tools-operator export-ignore
tools-operator/** export-ignore

# X-5 continued. The guard for X-5 immediately found what the prefix patterns above missed:
# docs/ has SUBDIRECTORIES, and `docs/CP-*` does not reach into them.
#
# docs/audit/ is ten build-programme audit records (oracle classification, gate evidence,
# ledger integrity, not-run causes, backfill, envelope, adversarial sample, the CP-M1 punch
# list, AUDIT-01 and SYSTEM-REVIEW). docs/evidence/spa/ is one discovery report from the SPA
# work and carries the developer username. Neither is referenced by any product file
# (measured across shell/, modules/ and tools/), and both are the build's record of itself.
docs/audit export-ignore
docs/audit/** export-ignore
docs/evidence export-ignore
docs/evidence/** export-ignore

# X-6 (EPC-01 P3-2) - THE BUILD HARNESS AT THE ROOT DOES NOT SHIP EITHER.
#
# X-5 cut tools-operator/ because "its six Start-OpenCode-*.ps1 files are build-harness
# launchers". Three files of exactly that class sit at the repository root and were missed,
# along with the two agent envelopes that govern how this repository is built. They are the
# running instructions for the builder, not documentation for a recipient, and between them
# they carry the developer username and the build tree's absolute path into every archive.
#
# Measured before excluding, across shell/src, shell/tests, tools/ and README.md: no product
# file references AGENTS.md or CLAUDE.md, and Start-OpenCode.ps1's only reference in the whole
# repository is inside docs/SOVEREIGN_CANONICAL_HANDOFF_20260827.md, which this same block
# excludes.
#
# NOT excluded, deliberately: BUILD-DIRECTIVE-SWS-UI-001.md. The shell SERVES it
# (shell/src/server.py:40, the "directive" doc route) and README.md links it, so cutting it
# would replace a disclosure with a broken route. It carries the build tree's name seven
# times and the builder envelope forbids the builder editing it, so the fix is the operator's
# to make. Recorded in release-planning/bundles/EPC01/PARKED-P3-2-build-directive.md rather
# than left as a silent exception.
#
# History stays tracked; only `git archive` output changes.
/AGENTS.md export-ignore
/CLAUDE.md export-ignore
/Start-Loop-Resume.ps1 export-ignore
/Start-OpenCode.ps1 export-ignore
/Start-OpenCode-FullAccess.ps1 export-ignore

# X-6 continued - three docs/ build records the X-5 prefix patterns did not reach.
# GATE5-REPORT is a gate evaluation, MASTER-RUN-ORDER is a build sequencing plan, and
# SOVEREIGN_CANONICAL_HANDOFF is a reviewer handoff carrying an unresolved findings table.
# None is referenced by any product file. All three are the build's record of itself, which
# is the line X-5 drew.
docs/GATE5-REPORT.md export-ignore
docs/MASTER-RUN-ORDER-* export-ignore
docs/SOVEREIGN_CANONICAL_HANDOFF_* export-ignore

# X-6 continued - three development-record files that NOTHING cites.
#
# The rest of modules/sow/docs/evidence stays, and that is a measured decision rather than
# deference to X-4: of the 28 flagged files in the module evidence trees, 25 are cited by
# something a recipient runs - test_evidence_receipts.py, run_phase19_gate.py, the desktop
# selfcheck scripts, the decision and unresolved-issue registers. Cutting those would trade a
# disclosure for a broken verification path. These three are cited by nothing at all.
modules/sow/docs/evidence/live/PHASE17E_RERUN1_RED_20260731T1506Z.json export-ignore
modules/sow/docs/evidence/receipts/PHASE18A_RUNNER_STATUS_R3.txt export-ignore
modules/distillery/runs/release-baseline/W1_EXPORT_BATTERY.json export-ignore
